{
  "name": "pinboard-pro",
  "version": "2.0.0",
  "private": true,
  "description": "Google Chrome extension for Pinboard.in (https://pinboard.in).",
  "author": "Richard Käll <richard.kall@me.com>",
  "scripts": {
    "build": "rm -rf dist && webpack",
    "format": "web-scripts format",
    "lint": "web-scripts lint"
  },
  "husky": {
    "hooks": {
      "pre-commit": "web-scripts pre-commit",
      "pre-push": "web-scripts pre-push --no-test --no-type-check"
    }
  },
  "devDependencies": {
    "@richardkall/web-scripts": "^0.7.3",
    "copy-webpack-plugin": "^5.1.1",
    "html-webpack-plugin": "^3.2.0",
    "husky": "^4.2.3",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.11",
    "zip-webpack-plugin": "^3.0.0"
  }
}
